vich_uploader:
    db_driver: orm

    mappings:
      category_image:
        uri_prefix: /img/categories
        upload_destination: '%kernel.project_dir%/public/img/categories'
      product_image:
        uri_prefix: /img/products
        upload_destination: '%kernel.project_dir%/public/img/products'
      creation_image:
        uri_prefix: /img/creation
        upload_destination: '%kernel.project_dir%/public/img/creation'
      background_image:
        uri_prefix: /img/background
        upload_destination: '%kernel.project_dir%/public/img/background'

    #mappings:
    #    products:
    #        uri_prefix: /images/products
    #        upload_destination: '%kernel.project_dir%/public/images/products'
